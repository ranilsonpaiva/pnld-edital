{
  "name": "pnld-edital",
  "private": true,
  "version": "0.1.0",
  "description": "Extração e validação HITL de critérios de editais PNLD",
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "preview": "npm run preview --prefix apps/web"
  }
}
